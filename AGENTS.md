# AGENTS.md

Guidance for AI agents working in this repo lives in **[CLAUDE.md](CLAUDE.md)** — read it.

It contains the project overview, repo orientation, the roadmap, key data/parameter
paths, and the project rules every change must follow (use `uv`; update `README.md` when
code changes affect its examples; run `uv run ruff format` before each commit; follow
Conventional Commits).
